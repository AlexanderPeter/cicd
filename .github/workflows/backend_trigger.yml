
name: Trigger backend build

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/backend_job.yml'
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/backend_job.yml'

jobs:
  job_build_backend:
    uses: ./.github/workflows/backend_job.yml
