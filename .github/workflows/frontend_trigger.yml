
name: Trigger frontend build

on:
  push:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend_job.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend_job.yml'

jobs:
  job_build_frontend:
    uses: ./.github/workflows/frontend_job.yml
